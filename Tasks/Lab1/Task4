/* Task4 Write a program that multiplies two real numbers (FLOAT type) entered by the user. 
Take care of the appropriate formatting of the result and messages guiding the user through data entry. */

#include<stdio.h>

int main(void){
float x, y;

printf("Enter the number: ");
scanf("%f", &x);

printf("Enter the second number: ");
scanf("%f", &y);

printf("The result of multiplication is: %.2f\n", x*y );

return 0;
}
