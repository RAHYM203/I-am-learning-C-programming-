/* Task 4. Ask the user to input two floating-point numbers. 
Multiply them and print the result with proper formatting. */

#include<stdio.h>

int main(void){
float x, y;

printf("Enter the number: ");
scanf("%f", &x);

printf("Enter the second number: ");
scanf("%f", &y);

printf("The result of multiplication is: %.2f\n", x*y );

return 0;
}
